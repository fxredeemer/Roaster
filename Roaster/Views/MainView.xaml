<adonis:AdonisWindow x:Class="Roaster.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:adonis="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
        xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:local="clr-namespace:Roaster.Views"
        mc:Ignorable="d"
        Title="Roaster" Height="350" Width="500" MinHeight="300" MinWidth="450">
    <DockPanel Margin="8" LastChildFill="True">
        <DockPanel DockPanel.Dock="Top" LastChildFill="True">
            <TextBlock Margin="0 0 8 0">Task Count</TextBlock>
            <TextBlock DockPanel.Dock="Right" Text="{Binding TaskCount}"/>
            <Slider x:Name="TaskCount" Minimum="1" Maximum="{Binding TotalThreadCount}" Margin="0 0 8 0"/>
        </DockPanel>
        <Button x:Name="StartStop" DockPanel.Dock="Top" Content="{Binding ButtonText, FallbackValue=Roast}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <lvc:CartesianChart DockPanel.Dock="Bottom" Margin="0, 0, 0, 0" Series="{Binding CPULoad}" Hoverable="False" DataTooltip="{x:Null}"/>
    </DockPanel>
</adonis:AdonisWindow>
